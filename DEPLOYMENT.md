# 部署与验证

## 分支关系

- `main`：保留原始源代码与 GitHub Actions 部署工作流。
- `portfolio-static-20260617`：当前可直接发布的静态站点快照。

`main` 分支中的部署工作流会检出静态快照分支，将站点文件复制到 Pages 构建目录并发布。这样可以避免用构建产物覆盖源代码历史。

## 发布前检查

1. 运行本地预览服务器。
2. 检查首页、个人项目、电赛项目和项目详情路由。
3. 检查 `1024x768`、`1180x820` 横屏平板布局。
4. 确认页面不存在横向滚动条和文字遮挡。
5. 执行 JavaScript 语法检查。
6. 确认 `sw.js`、关键 CSS、JS 和首屏图片均可访问。

## 发布流程

1. 在 `portfolio-static-20260617` 分支完成内容修改、图片接入和本地验证。
2. 提交并推送静态快照分支。
3. 在 `main` 分支触发 `Deploy to GitHub Pages` 工作流。工作流会重新检出静态快照分支并发布，不会用构建产物覆盖源码历史。
4. 等待 GitHub Pages 部署完成后，检查线上首页、个人项目列表和本次修改的项目详情页。

如果 GitHub CLI 已登录，可以直接触发：

```powershell
gh workflow run deploy.yml --ref main
```

如果没有 GitHub CLI 登录状态，可以在 `main` 分支推送一个明确的部署提交，或在 GitHub Actions 页面手动运行工作流。

## 弱网策略

- 首屏 HTML 内置轻量加载状态，主脚本下载期间不会显示空白页。
- 4 张高分辨率照片已按网页展示尺寸降采样并渐进压缩。
- Service Worker 缓存页面外壳、关键脚本、样式和首屏图片。
- 导航请求超过 4.5 秒时优先返回最近一次成功缓存。
- 图片加载失败时返回轻量占位图，不阻塞正文浏览。
- 检测到省流量或 2G 网络时，减少动画和毛玻璃效果。

## 线上地址

```text
https://203863488-cell.github.io/personal-site/
```
